# 选题体检站 · 施工计划（BUILD）

> 照着做的施工图。设计依据与证据在 `PLAN.md`（§14 对抗审查），地基验证在 `eval/M0-report.md`。
> **M0 已完成**，结论已并入本文。你从 M1 开始做。
> 版本 v2 · 机制 v3（双短语 AND）· 已过构念效度验证（37%，落在"继续但需强化"区间）

---

## 0. 动工前必须接受的四条

都是实测+人工标注结论，不是观点。违反任何一条会做出不可信的产品。

### ① 主方向必须是真实术语短语 —— 流程第一道闸

v3 的双短语口径有一个前置条件，此前没写明，M0 才发现：

| 主方向 | 加引号 | 无引号 | 性质 |
|---|---|---|---|
| `cyberbullying` | 23,536 | — | ✅ |
| `bearing fault diagnosis` | 8,309 | 17,868 | ✅ |
| `PCB defect detection` | 711 | 1,774 | ✅ |
| **`social media adolescent mental health`** | **9** | 5,506 | ❌ 拼凑 |
| **`gut microbiome depression`** | **8** | 2,417 | ❌ 拼凑 |

拼凑短语会让结果**整体归零**（37 个角度里 29 个落入"无意义组合"，A 组为空）。
而 v1/v2 的无引号口径**掩盖了这个问题**——`gut microbiome depression` 在 v2 下报 2,417 篇看着完全正常。

> ⚠️ 由此：**此前所有基于 `gut microbiome depression` 的 v2 结论都不可信。**

已实现于 `scan.py`：域量 <100 拒绝执行并给出截短建议，<500 警告样本偏少。

### ② 放弃「MFR 数值排序」

两头都不可用：

| | PCB（域量 711） | 社科（域量 1429） |
|---|---|---|
| MFR 跨度 | 10.6x~16.8x（**1.6 倍**，排不动） | — |
| 榜首是什么 | — | mixed methods 126x / RCT 110x / meta-analysis 92x ← **全是方法词** |

小方向区分度崩塌，大方向被"词元通用性"污染。`category=method` 隔离拦不住 `reproducibility`、`explainability`（`quality` 类）。

**界面上不出现 MFR 排行榜，不出现"第 1 名空位"。**

### ③ 产品是「四分类漏斗」，重心在 D 组

| 组 | 判据 | 可靠性 | M0 实测 |
|---|---|---|---|
| **D 自动排除** | 专研0 且提及 < 域量×1% | **最高** | PCB 22/44、bearing 6/34、社科 8/37；含全部假金矿 |
| **C 待核实线索** | 专研0 且提及 ≥ 域量×1%，且非 method | 中，**严格命中率仅 37%** | PCB 4/4、bearing 1/2、社科 **2/13** |
| **A 已被研究** | 专研 ≥ 5 | 高（只说"有人做过"，不排序） | — |
| **B 线索** | 0 < 专研 < 5 | 低（标注，不建议） | — |

**叙事重心是"先帮你划掉不用看的"，不是"帮你找到金矿"。**
一句话价值：「44 个角度里，22 个可以直接划掉，5 个值得你花一晚上查，剩下的已经有人做了。」

### ④ 「敢否决」与 MFR 无关，别混着卖

L4 否决只由用户自报条件（无GPU/无数据集/周期不足）的规则表触发，**不经过 OpenAlex**。

- **条件否决**（可以说）：「以你的条件做不完」
- **价值否决**（不可以说）：「这方向没价值」

---

## 1. 里程碑（当前进度）

```
[M0 ✅] ──> [M1 ✅] ──> [M2 ✅] ──> M3 LLM两端 ──> M4 界面 ──> M5 交付
                                     (1.5天)      (1.5天)    (1天)
```

**M0 / M1 / M2 已完成并验证。** 形态为网页：OpenAlex 开放 CORS，故
**纯前端直连、零后端**，且每位访客用自己 IP 的额度，天然分摊（后端代理做不到这点）。

| 已交付 | 产物 | 验证 |
|---|---|---|
| M0 地基验证 | `eval/M0-report.md` | C 组严格命中率 37%，逐项人工标注 |
| M1 计数引擎 | `web/engine.js`（纯逻辑，无 DOM） | `node test/run.js` **31/31 通过** |
| M2 四分类 | 同上（含分类、证据、免责） | 不变量断言全过；浏览器沙箱加载验证通过 |

实跑基线（PCB defect detection / 电子信息）：域量 711、44 角度、89 次查询、
A=5 B=12 C=4 D=23、失败 0。C 组输出 = 类别不平衡 / 数字孪生 / 主动学习 / 图神经网络，
**与 M0 人工标注的 4/4 完全一致**。

集成方式与"做界面时不要破坏的十条"见 `README.md`。

M0 结论摘要（详见 `eval/M0-report.md`）：C 组严格命中率 **7/19 = 37%**，落在 30%~50% 区间 → **继续，但三项强化为必须**：

1. LLM 对 C 组的二次筛从"可选"→ **必须**
2. C 组措辞降级为**"待核实线索"**，不叫"候选空位/金矿"
3. 主方向术语性校验作为流程第一闸（已实现）

---

## M1 计数引擎（1天）

把 `probe/scan.py` 固化为可被界面调用的服务。**算法已验证，照搬，不要重写。**

### 必须照搬的五条（每条都有教训，见 `PLAN.md` §14）

1. **双短语 AND**：`"<主方向>" "<角度>"`，分子分母同口径
   - 无引号会让 `PCB defect detection semantic communication` 退化为 token-AND，v2 因此产出 **7/7 全假**的金矿榜
   - 主方向不加引号自身虚高 2.5 倍（1774→711）
2. **术语性校验前置**：域量 <100 拒绝，<500 警告
3. **一律 `group_by=publication_year`**：1 credit vs 行查询 10 credit，且顺带拿到趋势曲线
4. **领域锚默认关闭**：实测冗余（排序完全不变，仅缩分母 20–40%），开着反而引入偏差
5. **查询失败绝不静默**：3 次退避重试；失败角度进 `failed_angles`；响应带 `is_complete`

### 数据结构

```jsonc
scan_query  { id, topic, angle, mode, count, by_year, query_url, fetched_at }
angle_score { topic, angle, category, focus_count, mention_count,
              mfr, group: "A"|"B"|"C"|"D", low_confidence, reason }
report      { id, topic, term_check, conditions{}, focus_total, by_year,
              groups{A[],B[],C[],D[]}, findings[],
              is_complete, failed_angles[], disclaimer }
```

### 验收标准

- [ ] 同一方向跑两次，A/B/C/D 分组一致（幂等）
- [ ] 拼凑短语（用 `social media adolescent mental health` 测）被术语校验拦下并给出截短建议
- [ ] 断网重跑：`is_complete=false` 且列出失败角度，**不产出残缺却看似完整的结果**
- [ ] 单方向成本 ≤ $0.015、查询数 ≤ 100（实测 PCB 89 次 / $0.0089）
- [ ] 缓存命中时不发新请求，且结果带原始 `fetched_at`（不可伪装成新鲜数据）

### 可选（推荐做）

- [ ] **别名合并**：用 `angle_dict.csv` 的 `aliases` 列对分子做并集去重。实测 `few-shot`=18 / `low-shot`=7 / `one-shot`=4 / `meta-learning`=6，分子被别名分散低估约 2 倍（`PLAN.md` 3.11）

---

## M2 四分类与报告（0.5天）

### 分类规则（直接实现，不要自创阈值）

```python
gap_thr = max(10, int(focus_total * 0.01))      # 相对阈值，禁止跨学科硬编码

if focus == 0 and mention >= gap_thr and category != "method":  group = "C"
elif focus == 0:                                                group = "D"
elif focus < 5:                                                 group = "B"
else:                                                           group = "A"
```

⚠️ `category != "method"` 必须写在**计算时**。只在展示层拦会让 JSON 里 `is_absolute_gap` 仍为 `true`——M0 实测 bearing 的 `meta-analysis`、社科的 `ethnography` 都这么漏出来。

### 三级证据标注（`PLAN.md` 铁律 3）

| 标注 | 用于 | 必须携带 |
|---|---|---|
| 🔵 实测 | 所有计数、分组 | 查询串原文 + 命中数 + `fetched_at` |
| 🟡 推断 | LLM 的线索合理性判断 | 它依据的 `scan_query.id` |
| ⚪ 未核实 | LLM 生成的研究背景/路线 | 显式标"未核实" |

**准入门槛**：没有查询串+命中数+时间戳的结论不准进报告。宁可少一条。

### 验收标准

- [ ] 每个数字可点开看到可重跑的 OpenAlex URL
- [ ] 总体结论用**最高风险级别**，不做加权总分（避免与 `scholar-evaluation` 同构）
- [ ] 无 MFR 排行榜；A 组只显示"已有 N 篇研究"
- [ ] 免责**双侧**：既说"线索不是新颖性保证"，也说"红海判定可能误伤真金矿"

---

## M3 LLM 两端（1.5天）

LLM 只在**首尾**出现，中间计数一步都不许它插手。

### 端一：候选角度生成

```
输入：主方向 + 学科
输出：20~30 个角度 {angle_en, angle_zh, category}
category ∈ {emerging, mainstream, task, method, deployment, quality}
```

- 学科无限制的关键：任何学科 LLM 都能生成，不必预维护词典
- **数据层天然过滤幻觉**：编造术语得到 `专研0/提及0` → 落入 D 组
- ⚠️ **必须按学科生成，不要套用通用 ML 角度**。M0 实测：社科 C 组的 6 个"无关"项全部来自通用层的 ML 角度（`active learning`/`transfer learning`/`digital twin`/`GNN`/`generative model`，提及 15–134，伪装成空位）
- `data/angle_dict.csv`（366 角度 / 27 学科）降级为兜底 + `category` 权威来源；已内置学科黑名单（社科/法学/传播/语言/教育/心理/管理 不并入通用层）

### 端二：C 组语义合理性复核 —— **必须做**

M0 严格命中率仅 37%，没有这一步会向学生推荐大量无关组合。

```
输入：主方向 + C组角度 + 专研/提及数 + 查询串
输出：{verdict: 真线索|弱相关|无关, reason, 必须引用给它的实测数字}
```

专治 `additive manufacturing × bearing fault diagnosis`（关系间接）、`digital twin × problematic social media use`（无关）这类。**LLM 不得修改任何计数**，输出标 🟡 推断。

### 结构化澄清（照抄 DeepTutor 硬约束）

`deeptutor/tools/ask_user.py` + `hints/en/ask_user.yaml`：

- 最多 **4 问**，每问最多 **8 选项**
- 选项 `{label, description}`，description 说明"选它意味着什么"
- 推荐项排首位标 `(Recommended)`；**禁止模型自造 "Other"**
- 每条消息最多一个提问；**不得把提问当回合结束**；不许问"要继续吗"

四问：研究方向 / 算力 / 数据 / 周期与目标。

### 验收标准

- [ ] 同一方向连续生成 3 次，角度重合度 ≥ 60%
- [ ] 幻觉术语 100% 落入 D 组（构造 3 个假术语测试）
- [ ] 社科方向生成的角度中，ML 技术词 ≤ 10%
- [ ] LLM 输出强制 JSON，解析失败有兜底
- [ ] C 组复核每条结论都引用了实测条目 id

---

## M4 界面（1.5天）

四屏，做完一屏能用再做下一屏。

| 屏 | 内容 | 关键点 |
|---|---|---|
| S0 提问 | 4 问结构化澄清 | 选项带 description；推荐项标注 |
| S1 确认+报价 | 复述理解让用户**改写**（结构可回传，不是点确认）；显示"将查 N 次 / 约 X 秒 / 约 $Y"；**术语校验在此拦截**并给截短建议 | 后端只给单位成本，前端本地累加 → 用户增删角度时估算实时更新而不发请求（`deeptutor/book/estimate.py`） |
| S2 扫描 | 逐格点亮的分组表 | 增量执行三条：立刻落盘、进度靠存储可见不靠内存、每批小到能成功原子到能重试 |
| S3 报告 | 四分类 + 趋势曲线 + 证据徽章 | 趋势数据在 M1 已零成本拿到（`by_year`） |

### 措辞硬规定（M0 结论）

- C 组标题：**"待核实线索"**，不叫"候选空位/金矿/蓝海"
- C 组每项旁固定一句："需你自己判断这个组合是否讲得通"
- 不得称"领域内分位"——分位是相对**当前词典**算的，加 20 个红海词所有分位都变。正确措辞："在本次扫描的 N 个角度中排第 k"

### 验收标准

- [ ] 扫描中途刷新，进度不丢
- [ ] 任一数字可点开 → 完整 OpenAlex URL + 抓取时间
- [ ] 报价与实际成本偏差 ≤ 20%
- [ ] 不完整扫描在顶部有醒目提示，不只在角落

---

## M5 交付（1天）

- [ ] 端到端跑通 3 个不同学科方向，截图留档
- [ ] `README` 写清：能做什么、**不能做什么**、数据来源、局限（`PLAN.md` §11 + 双侧免责）
- [ ] **把 M0 的 37% 命中率写进 README** —— 这是最有说服力也最诚实的东西
- [ ] 清理临时文件；密钥走服务端管理，不入库

---

## 2. 风险与对策

| 风险 | 触发信号 | 对策 |
|---|---|---|
| 用户输入拼凑短语 | 术语校验域量 <100 | 已实现拦截 + 截短建议（实测 `social media adolescent mental health` → 建议 `adolescent mental health`，16691 篇可用） |
| 小方向不可用 | 双短语后域量 <500 | 明确提示"只给分类不给对比" |
| C 组质量方向相关 | 命中率波动 100%→15% | LLM 二次筛已升为必须；措辞降级 |
| OpenAlex 限流 | `X-RateLimit-Remaining` 见底 | 缓存兜底；**重置是固定时间点非滚动窗口**（实测 Reset 43516s / 80471s），演示前一天别猛跑 |
| 别名漏检使分子低估 | 已知（`PLAN.md` 3.11） | M1 可选项：`aliases` 并集去重 |
| 中文方向 | 用户输入中文 | 前端拦截提示改英文（OpenAlex 以英文文献为主） |

---

## 3. 不做清单（防范围膨胀）

- ❌ 写开题报告 / 文献综述（官方点名方向，红海）
- ❌ 论文精读问答（DeepTutor 36k stars 已占）
- ❌ 跑代码跑实验（openscience 已占，需本地算力）
- ❌ 给选题打综合分 / 分档（`scholar-evaluation` 已有加权打分器，会被认作同构）
- ❌ MFR 排行榜（§0-② 已证不可用）

---

## 4. 工期

| 里程碑 | 工时 | 状态 |
|---|---|---|
| ~~M0 地基验证~~ | ~~0.5天~~ | ✅ 完成（`eval/M0-report.md`） |
| ~~M1 计数引擎~~ | ~~1天~~ | ✅ 完成（`web/engine.js`，31/31 测试通过） |
| ~~M2 四分类~~ | ~~0.5天~~ | ✅ 完成（含证据链与双侧免责） |
| M3 LLM 两端 | 1.5 天 | 待做，可与 M4 并行 |
| M4 界面 | 1.5 天 | 待做，只消费 engine 返回结构 |
| M5 交付 | 1 天 | 待做 |
| **剩余** | **约 3 天** | 2 人可压到 2 天 |

---

## 5. 现在就能开始的第一件事

M0/M1/M2 已完成。先跑一遍确认环境没问题：

```bash
cd 科研lab
node test/run.js --offline      # 纯逻辑自检，不联网
node test/run.js                # 全量自检（约 130 次查询）
start web\index.html            # 浏览器打开最小验证页
```

然后你的下一步是 **M3（LLM 两端）或 M4（界面）**，两者可并行：

- 做界面 → 只依赖 `engine.js` 的返回结构，**不要在页面里重写任何判定逻辑**；
  必读 `README.md` 的「做界面时不要破坏的十条」
- 做 LLM → 见下方 M3，其中 **C 组语义复核是必须项**（因命中率仅 37%）

现有资产：

| 文件 | 用途 |
|---|---|
| `PLAN.md` | 方案设计 + 全部实测证据 + 对抗审查记录（§14） |
| `BUILD.md` | 本文，施工图 |
| `eval/M0-report.md` | 地基验证报告，含 37% 命中率的逐项标注 |
| `web/engine.js` | **核心引擎**（JS，纯逻辑无 DOM，浏览器+Node 双环境） |
| `web/angles.js` | 词典的 JS 形态，由 `tools/gen_angles.py` 生成 |
| `web/index.html` | 最小验证页（只调引擎，不重写逻辑） |
| `test/run.js` | 引擎自检 31 用例 |
| `README.md` | 集成说明 + 做界面时不要破坏的十条 |
| `probe/scan.py` | Python 版同机制引擎（用于批量实验与交叉验证） |
| `data/angle_dict.csv` | 366 角度 / 27 学科 + 通用层（含学科黑名单） |
| `out/*.json` | 历史扫描证据，含每条查询串与时间戳 |
| `probe/scan_v2_anchored.py.bak` | v2 锚定版备份（对照用） |
